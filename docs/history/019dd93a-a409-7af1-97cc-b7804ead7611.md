# Bản lưu nội dung phiên 019dd93a-a409-7af1-97cc-b7804ead7611

Nguồn: Codex task ID `019dd93a-a409-7af1-97cc-b7804ead7611`.

Tên nguồn: Tích hợp render TikZ

Workspace nguồn: `/Users/hoangblue38/Desktop/website trac nghiem`.

Ngày tổng hợp: 2026-09-24. Đã đọc hết phân trang được API cung cấp (hasMore=false).

Đây là bản lưu các tin nhắn người dùng và trợ lý có thể truy xuất, theo thứ tự thời gian; không bao gồm suy luận nội bộ, tool output hay toàn bộ diff. Khẳng định kỹ thuật, tên mô hình, URL localhost và kết quả kiểm tra bên dưới thuộc thời điểm phiên cũ, không phải xác nhận hiện tại. Không dùng đề xuất cũ làm cấu hình triển khai trước khi kiểm chứng lại.

## Lượt 019dd93a-aae7-77a1-905e-5817eb76573e

Trạng thái nguồn: completed. Thời điểm kết thúc (UTC): 2026-04-29T12:40:33.000Z.

### Người dùng

Đóng vai là Chuyên gia Next.js 14/15 và Tích hợp API (API Integration). Tôi vừa xây dựng thành công một Microservice riêng trên Hugging Face để render mã TikZ thành ảnh PNG (trả về chuỗi Base64).

Bây giờ tôi cần tích hợp nó vào luồng Upload đề thi trên Next.js (Server-side) để tiền xử lý toàn bộ mã TikZ thành ảnh trước khi lưu vào Database (Firebase).

Thông tin API của tôi:
- Endpoint: `[https://frankii1990-tikz-render.hf.space/render]` (Method: POST)
- Payload gửi đi: `{"tikz_code": "\\begin{tikzpicture}...\\end{tikzpicture}"}`
- Response nhận về: `{"status": "success", "image_base64": "iVBORw0KGgo..."}`

Nhiệm vụ 1: Viết hàm tiện ích `processTikzToImages(content: string)`
- Hàm này chạy ở môi trường Server (ví dụ trong API Route hoặc Server Action).
- Sử dụng Regex để tìm TẤT CẢ các khối text bắt đầu bằng `\begin{tikzpicture}` và kết thúc bằng `\end{tikzpicture}` (bao gồm cả 2 thẻ này).
- Dùng vòng lặp (như `for...of` để xử lý tuần tự hoặc `Promise.all` nếu gọi song song) để trích xuất từng mã TikZ và gọi `fetch` POST đến Endpoint trên.
- Khi nhận được `image_base64`, hãy thay thế toàn bộ khối TikZ gốc trong chuỗi `content` bằng cú pháp thẻ hình ảnh Base64 của HTML: `<img src="data:image/png;base64,..." alt="TikZ Diagram" className="tikz-image mx-auto my-2 max-w-full" />` (hoặc cú pháp Markdown ảnh nếu phù hợp với hệ thống render hiện tại).
- Trả về chuỗi `content` mới đã sạch bóng mã TikZ và chỉ còn chứa thẻ ảnh.

Nhiệm vụ 2: Áp dụng vào luồng Upload
- Hãy cung cấp một đoạn code mẫu (ví dụ API Route `POST /api/upload-quiz` hoặc Server Action) để tôi thấy rõ cách gọi hàm `processTikzToImages` trước khi lưu dữ liệu `cleanedContent` xuống Database.
- Có xử lý lỗi `try...catch` đầy đủ đề phòng trường hợp API bên Hugging Face bị timeout, nếu lỗi ở 1 hình thì giữ nguyên code TikZ gốc của hình đó hoặc văng log lỗi thay vì làm sập toàn bộ luồng upload.

Hãy viết code TypeScript sạch sẽ, module hóa và chú thích rõ ràng.


### Trợ lý (commentary)

Tôi sẽ đọc nhanh cấu trúc dự án và tài liệu Next đi kèm trong `node_modules` trước, vì repo này có cảnh báo phiên bản Next khác các giả định thông thường. Sau đó tôi sẽ thêm tiện ích xử lý TikZ và gắn vào một điểm upload phù hợp nếu đã có sẵn.

### Trợ lý (commentary)

Tôi thấy dự án đang lưu đề trực tiếp từ các trang admin client-side vào Firestore, đồng thời đã có API `/api/process-tikz`. Tôi sẽ kiểm tra các phần đó để tích hợp đúng kiểu dữ liệu hiện tại thay vì đưa một ví dụ rời rạc.

### Trợ lý (commentary)

Tôi sẽ cập nhật module xử lý TikZ hiện có để gọi Hugging Face thay vì QuickLaTeX, giữ lại API cũ (`convertTikzToImage`) để các màn hình hiện tại không phải đổi nhiều. Tôi cũng sẽ thêm đúng hàm `processTikzToImages(content)` cho trường hợp nội dung còn nguyên các block `tikzpicture`.

### Trợ lý (commentary)

Tôi sẽ thêm một Route Handler `/api/upload-quiz` đúng luồng server-side: client gửi đề đã parse, server chuyển TikZ sang ảnh rồi mới ghi Firestore. Phần tạo đề sẽ gọi route này thay cho việc tự ghi Firestore từ client.

### Trợ lý (commentary)

Phần tạo đề đã chuyển sang gọi `/api/upload-quiz`. Tôi sẽ chạy kiểm tra lint/build để bắt lỗi TypeScript hoặc import trước khi chốt.

### Trợ lý (commentary)

Lint toàn repo đang thất bại bởi lỗi có sẵn ở nhiều file ngoài phạm vi thay đổi, kể cả thư mục `.claude/worktrees`. Tôi sẽ kiểm tra riêng các file vừa sửa để tách lỗi mới khỏi nợ lint hiện tại.

### Trợ lý (commentary)

Các file tôi vừa chạm qua lint riêng đã sạch. Tôi sẽ chạy build để kiểm tra type và Next route compilation; nếu build vấp ở lỗi sẵn có ngoài phạm vi, tôi sẽ ghi rõ.

### Trợ lý (commentary)

Build vẫn đang ở bước production compile của Next/Turbopack. Tôi đang đợi kết quả cuối cùng để không kết luận sớm.

### Trợ lý (commentary)

Build đã qua sau khi cho phép network để Next tải Google Fonts. Tôi sẽ khởi động dev server để bạn có thể thử luồng upload ngay trong app.

### Trợ lý (final_answer)

Đã tích hợp server-side TikZ preprocessing vào luồng tạo đề.

Các thay đổi chính:
- Thêm `processTikzToImages(content)` và `processTikzToImagesWithStats(...)` tại [src/utils/tikzToImage.ts](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/utils/tikzToImage.ts:141).
- `convertTikzToImage(...)` giờ gọi Hugging Face endpoint `https://frankii1990-tikz-render.hf.space/render`, nhận `image_base64`, rồi trả về `data:image/png;base64,...`.
- Thêm API Route server-side [src/app/api/upload-quiz/route.ts](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/app/api/upload-quiz/route.ts:116): xử lý TikZ trong `questionText`, `options`, `explanation`, `tikzCode`, `explanationTikzCode`, sau đó mới lưu Firestore.
- Cập nhật màn tạo đề để gọi `/api/upload-quiz` thay vì ghi Firestore trực tiếp từ client: [src/app/admin/create-exam/page.tsx](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/app/admin/create-exam/page.tsx:130).

Xử lý lỗi đã có theo yêu cầu: nếu một hình TikZ render lỗi hoặc timeout thì log lỗi, tăng `failedCount`, giữ TikZ gốc hoặc cơ chế render cũ cho hình đó, không làm sập toàn bộ upload.

Kiểm tra:
- `npx eslint src/utils/tikzToImage.ts src/app/api/upload-quiz/route.ts src/app/admin/create-exam/page.tsx src/app/api/process-tikz/route.ts` pass.
- `npm run build` pass sau khi cho phép network để Next tải Google Fonts.
- `npm run lint` toàn repo vẫn fail vì các lỗi có sẵn ngoài phạm vi thay đổi.

Dev server đang chạy tại: [http://localhost:3001](http://localhost:3001)

## Lượt 019dd951-640b-75a3-91d8-97d19f7d45af

Trạng thái nguồn: completed. Thời điểm kết thúc (UTC): 2026-04-29T12:58:35.000Z.

### Người dùng

A tree hydrated but some attributes of the server rendered HTML didn't match the client properties. This won't be patched up. This can happen if a SSR-ed Client Component used:
- A server/client branch `if (typeof window !== 'undefined')`.
- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

See more info here: https://nextjs.org/docs/messages/react-hydration-error

<HotReload globalError={[...]} webSocket={WebSocket} staticIndicatorState={{pathname:null, ...}}>
      <AppDevOverlayErrorBoundary globalError={[...]}>
        <ReplaySsrOnlyErrors>
        <DevRootHTTPAccessFallbackBoundary>
          <HTTPAccessFallbackBoundary notFound={<NotAllowedRootHTTPFallbackError>}>
            <HTTPAccessFallbackErrorBoundary pathname="/quiz/cat4..." notFound={<NotAllowedRootHTTPFallbackError>} ...>
              <RedirectBoundary>
                <RedirectErrorBoundary router={{...}}>
                  <Head>
                  <__next_root_layout_boundary__>
                    <SegmentViewNode type="layout" pagePath="layout.tsx">
                      <SegmentTrieNode>
                      <link>
                      <script>
                      <script>
                      <script>
                      <script>
                      <script>
                      <script>
                      <RootLayout>
                        <html
                          lang="vi"
                          className="geist_a71539c9-module__T19VSG__variable geist_mono_8d43a2aa-module__8Li5zG__varia..."
-                         __gcrremoteframetoken="2383f4c5b631508f887249f783bb24c8"
                        >
                  ...

web đã chạy rất oke, đã thành công render code tikz thành hình ảnh nhưng trên mobile báo lỗi như tôi đã paste ở trên. Hãy phân tích nguyên nhân và cho hướng xử lý


### Trợ lý (commentary)

Tôi sẽ kiểm tra `layout.tsx` và các component chạy trên trang quiz để xem có nhánh client/server hoặc dữ liệu động nào thật sự gây mismatch không. Dòng diff đang chỉ vào attribute lạ trên thẻ `<html>`, nên khả năng cao là môi trường trình duyệt/mobile đã tự chèn attribute trước khi React hydrate, nhưng tôi sẽ xác nhận bằng code.

### Trợ lý (final_answer)

Nguyên nhân chính trong log này không phải TikZ.

Dòng quan trọng là:

```diff
<html
  lang="vi"
  className="geist_..."
- __gcrremoteframetoken="2383f4c5b631508f887249f783bb24c8"
>
```

Attribute `__gcrremoteframetoken` không có trong code của app. Trong [src/app/layout.tsx](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/app/layout.tsx:27), thẻ `<html>` chỉ render `lang` và `className`. Vì vậy attribute này gần như chắc chắn bị browser/in-app browser/extension/webview chèn vào DOM trước khi React hydrate. React thấy HTML thực tế trên máy client khác HTML server gửi xuống nên báo hydration mismatch.

Hiện tại bạn đã có:

```tsx
<body suppressHydrationWarning>
```

nhưng lỗi nằm ở `<html>`, không phải `<body>`. Vì vậy hướng xử lý thực dụng nhất là thêm `suppressHydrationWarning` cho `<html>`:

```tsx
<html
  lang="vi"
  suppressHydrationWarning
  className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
>
```

Đây là cách hợp lý khi mismatch đến từ attribute ngoài app chèn vào root element. Nó chỉ “bỏ qua cảnh báo” ở cấp đó, không che lỗi UI sâu bên trong.

Ngoài ra vẫn có 2 điểm nên harden thêm để tránh hydration thật sự:

1. Trong `QuizClient`, `IntroScreen` có `const now = new Date();` ngay trong render. Client Component vẫn có thể được pre-render trên server, nên trạng thái “chưa mở đề / đã đóng đề” có thể lệch nếu thời điểm server và client khác nhau.
2. Các hàm format ngày như `toLocaleString("vi-VN")` cũng có thể lệch giữa server và mobile nếu timezone/locale khác.

Hướng xử lý tốt hơn cho phần thời gian: tính `now` sau khi component mount, hoặc truyền snapshot thời gian từ server xuống. Ví dụ đơn giản:

```tsx
const [mounted, setMounted] = useState(false);

useEffect(() => {
  setMounted(true);
}, []);

if (!mounted) {
  return null; // hoặc skeleton ổn định
}
```

Nhưng với log bạn paste, việc cần làm trước là thêm `suppressHydrationWarning` vào `<html>`. Sau đó test lại trên mobile bằng Safari/Chrome thật, tránh Facebook/Zalo/in-app browser. Nếu warning biến mất và UI vẫn chạy ổn thì đây là mismatch do môi trường trình duyệt chèn attribute, không phải lỗi luồng render TikZ.

## Lượt 019dd954-7bc6-7750-a3d3-2e520f95d7b5

Trạng thái nguồn: completed. Thời điểm kết thúc (UTC): 2026-04-29T13:01:36.000Z.

### Người dùng

Đồng ý sửa


### Trợ lý (commentary)

Tôi sẽ sửa trực tiếp `layout.tsx` để bỏ qua mismatch ở thẻ `<html>`, rồi kiểm tra nhanh file và build/type nếu cần.

### Trợ lý (commentary)

Đã thêm `suppressHydrationWarning` vào `<html>`. Tôi sẽ chạy lint riêng cho file layout và kiểm tra dev server vẫn phản hồi.

### Trợ lý (final_answer)

Đã sửa [src/app/layout.tsx](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/app/layout.tsx:31): thêm `suppressHydrationWarning` vào thẻ `<html>`.

```tsx
<html
  lang="vi"
  suppressHydrationWarning
  className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
>
```

Kiểm tra `npx eslint src/app/layout.tsx` đã pass.

Dev server hiện vẫn chạy ở [http://localhost:3001](http://localhost:3001). Hãy refresh lại trên mobile; nếu warning cũ chỉ do attribute `__gcrremoteframetoken` bị trình duyệt/webview chèn vào `<html>`, cảnh báo đó sẽ hết.

## Lượt 019dd967-d461-7f31-baba-43ee3d0735d3

Trạng thái nguồn: completed. Thời điểm kết thúc (UTC): 2026-04-29T13:26:31.000Z.

### Người dùng

Đóng vai là Chuyên gia UI/UX Next.js và Xử lý dữ liệu. Hệ thống thi trắc nghiệm của tôi đang gặp 2 lỗi cần bạn xử lý triệt để:

Nhiệm vụ 1: Fix lỗi giao diện bảng `tabular` (LaTeX) render ra HTML bị tràn màn hình Mobile và cả màn hình PC khi lưu đề thi.
- Hiện tại, khi thư viện LaTeX (KaTeX/react-latex-next) render các thẻ `\begin{tabular}`, bảng HTML tạo ra quá rộng, làm vỡ khung giao diện trên điện thoại.
- Yêu cầu:
  + Tìm Component đang hiển thị nội dung câu hỏi (nơi chứa component `<Latex>`).
  + Bọc khối hiển thị đó bằng một thẻ div có class của Tailwind CSS: `<div className="overflow-x-auto overflow-y-hidden max-w-full">...</div>` (hoặc viết CSS cục bộ cho class `.katex-display { overflow-x: auto; }`).
  + Đảm bảo người dùng trên điện thoại có thể vuốt ngang ngón tay để xem hết bảng mà không làm vỡ layout của toàn trang.

Nhiệm vụ 2: VIẾT LẠI HOÀN TOÀN logic render Bảng kết quả trong HTML Email
Code tạo HTML gửi Email của bạn viết lần trước bị lặp code cũ lộn xộn: Phần 2 (P2) in ra "SSSSSSSSSS" (10 chữ S); Phần 3 (P3) vẫn dính chuỗi LaTeX $5{,}3$.

Yêu cầu BẮT BUỘC: XÓA SẠCH logic lặp `<tbody>` cũ trong file API gửi Email (file route.ts) và VIẾT LẠI CẤU TRÚC các hàng (<tr>) theo chuẩn 5 cột rõ ràng: `Câu | Phần | Kết quả | Bạn trả lời | Đáp án đúng`.

1. Khai báo 2 hàm tiện ích ở đầu file/hàm sinh Email:
const cleanP3Answer = (ans) => {
  if (typeof ans !== 'string') return ans;
  return ans.replace(/\$/g, '').replace(/\{,\}/g, ',');
};

const getP2String = (ansObj) => {
  if (!ansObj) return '----';
  return ['a', 'b', 'c', 'd'].map(opt => {
    if (ansObj[opt] === true) return 'Đ';
    if (ansObj[opt] === false) return 'S';
    return '-';
  }).join('');
};

2. Cấu trúc render từng dòng <tr> (Tuân thủ chuẩn 5 cột HTML <td>):
- Nếu là Câu hỏi Phần 1 (P1):
  + KẾT QUẢ: In ra "Đúng" hoặc "Sai".
  + BẠN TRẢ LỜI: In chữ cái A, B, C hoặc D.
  + ĐÁP ÁN: In chữ cái A, B, C hoặc D.

- Nếu là Câu hỏi Phần 2 (P2) (TUYỆT ĐỐI KHÔNG xài lại code thừa cũ):
  + KẾT QUẢ: Chỉ in chuỗi `[số_ý_đúng]/4 ý đúng`.
  + BẠN TRẢ LỜI: Gọi hàm `getP2String(studentAns)` -> ra 4 ký tự (VD: ĐS-Đ).
  + ĐÁP ÁN: Gọi hàm `getP2String(correctAns)` -> ra 4 ký tự (VD: ĐĐĐS).

- Nếu là Câu hỏi Phần 3 (P3):
  + KẾT QUẢ: In "Đúng" hoặc "Sai".
  + BẠN TRẢ LỜI: In đáp án của học sinh (nhớ gọi cleanP3Answer nếu cần).
  + ĐÁP ÁN: BẮT BUỘC gọi `cleanP3Answer(correctAns)` để xóa LaTeX (VD: 5,3).

Hãy cung cấp lại đoạn CSS/UI fix cho Nhiệm vụ 1 và TOÀN BỘ file API gen Email HTML đã được viết mới cho Nhiệm vụ 2. Không để sót lại code sinh ra các thẻ <td> dư thừa cũ.


### Trợ lý (commentary)

Tôi sẽ xử lý trực tiếp hai điểm: trước hết tìm pipeline render LaTeX/tabular để khóa overflow ở một chỗ dùng chung, sau đó viết lại phần tạo bảng email trong API gửi kết quả để không còn reuse logic cũ.

### Trợ lý (commentary)

Tôi thấy API email hiện không còn đủ dữ liệu để in P2 dạng `ĐS-Đ` vì `QuizClient` đang gửi chuỗi “2/4 ý đúng” thay vì đáp án từng ý. Tôi sẽ sửa cả payload gửi email từ `QuizClient` để API nhận đúng object P2, rồi viết lại route email theo đúng 5 cột.

### Trợ lý (commentary)

Tôi sẽ làm 3 sửa đổi liên quan nhau: thêm lớp chống tràn cho mọi output KaTeX/tabular, đổi payload P2 gửi email sang object `{a,b,c,d}`, và thay toàn bộ logic render rows trong `send-result/route.ts` bằng renderer 5 cột duy nhất.

### Trợ lý (commentary)

Đã thay route email bằng cấu trúc mới. Tôi sẽ chạy lint riêng các file vừa sửa để bắt lỗi TypeScript/ESLint trước khi build.

### Trợ lý (commentary)

Lint riêng không có lỗi, chỉ còn warning `<img>` hiện có trong `QuizClient` và warning ESLint bỏ qua CSS. Tôi sẽ chạy build để xác nhận TypeScript với kiểu payload P2 mới.

### Trợ lý (commentary)

Build đang chạy qua bước compile của Next/Turbopack. Tôi đang đợi kết quả cuối để bắt cả lỗi route API nếu có.

### Trợ lý (final_answer)

Đã xử lý triệt để 2 lỗi.

**UI Tabular/KaTeX**
- Sửa [src/utils/textProcessor.tsx](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/utils/textProcessor.tsx:58):
  - `tabular` giờ được bọc bằng `overflow-x-auto overflow-y-hidden max-w-full`.
  - Bảng dùng `min-w-max max-w-full` để bảng rộng thì cuộn ngang, không kéo vỡ layout.
  - Các block KaTeX cũng được bọc overflow ngang.
- Thêm CSS global trong [src/app/globals.css](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/app/globals.css:21):

```css
.katex-display {
  max-width: 100%;
  overflow-x: auto;
  overflow-y: hidden;
  padding-bottom: 0.25rem;
}

.katex,
.katex-html {
  max-width: 100%;
}
```

**Email Result API**
- Viết lại toàn bộ [src/app/api/send-result/route.ts](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/app/api/send-result/route.ts:1).
- Xóa logic render row cũ bị lẫn P2/P3.
- Bảng chi tiết email giờ chỉ có đúng 5 cột:
  `Câu | Phần | Kết quả | Bạn trả lời | Đáp án đúng`
- P2 dùng `getP2String(...)`, không còn lỗi kiểu `SSSSSSSSSS`.
- P3 dùng `cleanP3Answer(...)`, nên `$5{,}3$` sẽ thành `5,3`.

Tôi cũng sửa payload gửi email trong [src/components/QuizClient.tsx](/Users/hoangblue38/Desktop/website%20trac%20nghiem/src/components/QuizClient.tsx:355) để P2 gửi object `{ a, b, c, d }`, vì API không thể in `ĐS-Đ` nếu client chỉ gửi chuỗi “2/4 ý đúng”.

Kiểm tra:
- Lint riêng các file sửa: không có error.
- `npm run build`: pass sau khi cho phép network để Next tải Google Fonts.

## Lượt 019dd96f-0d87-73f1-8be2-c86edd281507

Trạng thái nguồn: completed. Thời điểm kết thúc (UTC): 2026-04-29T13:30:29.000Z.

### Người dùng

Đóng vai là Chuyên gia React/Next.js và Mobile UI/UX. Tôi cần tối ưu hóa triệt để ô nhập liệu (Input) cho các câu hỏi Trả lời ngắn (Phần III) để mang lại trải nghiệm tốt nhất trên điện thoại và chặn người dùng nhập sai định dạng.

Hãy tìm Component chứa ô `<input>` để nhập đáp án của Phần 3 và thực hiện 2 nâng cấp BẮT BUỘC sau:

Nhiệm vụ 1: Tối ưu bàn phím Mobile (Tự động mở phím số)
- Thêm các thuộc tính sau vào thẻ `<input>` để ép điện thoại (đặc biệt là iOS Safari) tự động mở bàn phím số có hỗ trợ ký tự đặc biệt:
  `type="text"`
  `inputMode="decimal"`
- TUYỆT ĐỐI KHÔNG dùng `type="number"` vì HTML5 `type="number"` sẽ vô hiệu hóa việc nhập dấu phẩy (,) trên rất nhiều trình duyệt.

Nhiệm vụ 2: Bẫy Regex chặn ký tự ngay lúc gõ (Real-time Validation)
- Cập nhật hàm xử lý sự kiện `onChange` của ô input để thực hiện ngay 2 việc này trước khi set vào State:
  1. Tự động chuyển đổi dấu chấm `.` thành dấu phẩy `,` (ví dụ: gõ "5.3" tự biến thành "5,3").
  2. Lọc bỏ TẤT CẢ các ký tự chữ cái (ABC) hoặc ký tự lạ. CHỈ cho phép giữ lại: các chữ số từ `0` đến `9`, dấu trừ `-`, và dấu phẩy `,`.

Gợi ý logic hàm onChange:
```javascript
const handleInputChange = (e) => {
  // 1. Đổi dấu chấm thành phẩy
  let val = e.target.value.replace(/\./g, ',');
  // 2. Lọc bỏ mọi thứ không phải số, dấu phẩy, dấu trừ
  val = val.replace(/[^0-9,\-]/g, '');

  // Gọi hàm cập nhật state (ví dụ: onAnswerChange(val))
};
